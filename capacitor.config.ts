import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.mobiletickets',
  appName: 'MobileTicketsIonic',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
